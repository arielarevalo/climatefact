% Diagrama 1: Pipeline de Inferencia en Tiempo de Ejecución
\begin{tikzpicture}[node distance=1.1cm]
  % Nodos
  \node[io] (input) {Afirmación de entrada};
  \node[process, below=of input] (seg) {Segmentación de oraciones};
  \node[process, below=of seg, draw=black!60, dashed, fill=black!3] (ret)
    {Recuperación híbrida};
  \node[process, below=of ret] (nli) {Detección de contradicciones};
  \node[process, below=of nli] (gen) {Generación del reporte};
  \node[io, below=of gen] (output) {Reporte de verificación};

  % Flechas
  \draw[arrow] (input) -- (seg);
  \draw[arrow] (seg) -- (ret);
  \draw[arrow] (ret) -- (nli);
  \draw[arrow] (nli) -- (gen);
  \draw[arrow] (gen) -- (output);

  % Anotaciones
  \node[annot, left=0.4cm of ret] {ver Fig.~\ref{fig:retrieval}};
  \node[annot, left=0.4cm of nli] {DeBERTa-Large-MNLI};
  \node[annot, left=0.4cm of gen] {GPT-4o-mini};
\end{tikzpicture}
